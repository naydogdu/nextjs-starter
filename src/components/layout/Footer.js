//import {useInView} from "react-intersection-observer"

const Footer = (props) => {
    //const { ref, inView } = useInView()
    return (
        <footer className={["relative py-8 text-white", props.css].join(' ')}>

        </footer>
    )
}

export default Footer
